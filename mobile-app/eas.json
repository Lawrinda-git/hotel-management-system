{
  "cli": {
    "version": ">= 3.25.0"
  },
  "build": {
    "development": {
      "distribution": "internal",
      "android": {
        "gradleCommand": "app:assembleDebug"
      }
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "gradleCommand": "app:assembleDebug"
      }
    },
    "production": {
      "android": {
        "gradleCommand": "app:assembleRelease"
      }
    }
  }
}